# تعليمات المستخدم — دليل الاستخدام

## مقدمة
هذا الملف يحتوي على جميع الطلبات والحلول والتعليمات الخاصة بالمشروع. بعد كل تعديل، اعمل **`Ctrl+Shift+R`** (hard refresh) في المتصفح عشان الكاش يتحدث.

---

## 1. إصلاح حقل كود تليجرام

### المشكلة
في تليجرام بيقلي "a code was previously sent insert it below" ولكن خانه كتابه الكود غير موجوده.

### الحل
**الملف**: `frontend/assets/js/utils.js`
**السبب**: دالتي `show()` و `hide()` كانوا يستخدمون `qs()` التي تستعمل `document.querySelector(selector)` بدون إضافة `#`. لذلك `show('tg-form-code')` كان يبحث عن عنصر `<tg-form-code>` (tag) بدلاً من `id="tg-form-code"` ويفشل بصمت.
**الحل**: تغيير `show()` و `hide()` و `setHTML()` و `setText()` لاستخدام `document.getElementById(id)` بدلاً من `qs(id)`.

### كيفية الاستخدام
1. افتح تبويب Telegram
2. أدخل رقم الهاتف → Send Code
3. إذا ظهرت رسالة "⚠️ A code was previously sent — enter it below"، اكتب الكود في الحقل الذي سيظهر
4. اضغط Verify

---

## 2. إصلاح Website Downloader

### المشكلة
في Website Downloader ظاهر جمبها "Checking..." ومفيش عمليات تنزيل بادئه.

### الحل
**الملف**: `frontend/assets/js/utils.js` + `frontend/assets/js/website.js`
**السبب**: ملف `website.js:9` يسحب `$` من `Utils` ولكن `$` لم يكن موجوداً في `utils.js` — لا كدالة ولا في التصدير. كل `$('#website-status')` يرمي `TypeError`.
**الحل**: إضافة `const $ = id => document.getElementById(id)` وتصديره.

### كيفية الاستخدام
1. افتح تبويب Website DL
2. انتظر حتى تظهر "✅ wget available" أو "❌ wget not found"
3. أدخل رابط الموقع
4. اضغط "⬇ Download Website"
5. تابع التقدم من خلال进度 البار والـ live log
6. عند الانتهاء، اضغط "📦 Download ZIP"

---

## 3. إصلاح خطأ YouTube API Validation

### المشكلة
عند validating youtube api: "the requests use of the `mine` parameter is not supported".

### الحل
**الملف**: `backend/routes/settings_api.py`
**السبب**: YouTube API لا يسمح باستخدام parameter `mine` مع API keys (يحتاج OAuth).
**الحل**: تغيير endpoint من `channels?mine=false` إلى `videos?chart=mostPopular` (يدعم API keys).

### كيفية الاستخدام
1. اذهب إلى Settings → Integrations
2. أدخل YouTube API key
3. اضغط "Validate" — الآن يجب أن تظهر "✅ Valid YouTube API key"
4. اضغط "💾 Save" لحفظه

---

## 4. إضافة زر Save لـ YouTube API Key + منتقي البلاي ليست

### المطلوب
1. زر حفظ بجوار زر Validate
2. عند تحميل قنوات يوتيوب في وضع البلاي ليست: عرض البلاي ليست مع عدد الفيديوهات و check boxes

### الحل

#### الزر Save
**الملفات**: `frontend/index.html:1132` + `frontend/assets/js/settings.js:128,295-325`
- أضيف زر "💾 Save" بجانب "Validate"
- عند الضغط: يحفظ الـ API key في الإعدادات محلياً (`localStorage`) وعلى السيرفر (`API.saveSettings`)

#### منتقي البلاي ليست
**الملف**: `frontend/assets/js/downloader.js`
**التغييرات**:
- عند الضغط على Download ومود القناة من المودات اللي فيها playlists: يتم جلب البلاي ليست أولاً
- عرض نافذة منبثقة (modal) تحتوي على:
  - ✅ **Checkboxes** لكل بلاي ليست
  - ✅ **عدد الفيديوهات** (`p.video_count`)
  - ✅ **Select All / Deselect All**
  - ✅ **زر "Download Selected"** لتأكيد الاختيار
- للمود `playlists_and_uploads` و `all_uncategorized`: يتم أيضاً تحميل uploads القناة بعد البلاي ليست المحددة

### كيفية الاستخدام
1. الصق رابط قناة يوتيوب (مثل `https://www.youtube.com/@channel`)
2. اختر وضع مثل "📂 Playlists only"
3. اضغط **Fetch Info** أو **⬇️ Download**
4. ستظهر نافذة بكل البلاي ليست مع أعداد الفيديوهات
5. اختر اللي تريد بالـ checkboxes
6. اضغط "⬇ Download Selected"
7. سيبدأ التحميل

---

## 5. إصلاح خطأ `sqlite3.ProgrammingError: type 'dict' is not supported`

### المشكلة
عند فشل تحميل قناة يوتيوب، يظهر الخطأ:
```
sqlite3.ProgrammingError: Error binding parameter 3: type 'dict' is not supported
```

### الحل
**الملف**: `backend/database/db.py`
**السبب**: `_on_error` في `queue.py:178` يمرر `{'options': opts}` حيث `opts` هو `dict`. SQLite لا يدعم ربط `dict`.
**الحل**: إضافة auto-serialize في `update_job()` — لو القيمة `dict` أو `list`، نحولها لـ JSON قبل binding.

---

## 6. إصلاح Channel Info Extract Timeout

### المشكلة
```
Channel info extract failed: Command '[...]--dump-json --socket-timeout 15' timed out after 30 seconds
```

### الحل
**الملف**: `backend/services/ytdlp_service.py` (سطر 684 و 953)
**السبب**: `--socket-timeout 15` و `timeout=30` كانوا hardcoded.
**الحل**: استخدام `opts.get('timeout', self.settings.get('timeout', 30))` مع زيادة `subprocess timeout` بمقدار 15 ثانية.

---

## 7. أوامر التشغيل

### تشغيل المشروع
```bash
bash run.sh
# أو
cd backend && python app.py
```

### تشغيل الاختبارات
```bash
python -m pytest tests/ -q
```

### فحص أخطاء JavaScript
```bash
node --check frontend/assets/js/utils.js
```

---

## 8. الملخص الكامل — الملفات المعدلة في هذه الجلسة

| الملف | التعديل |
|-------|---------|
| `frontend/assets/js/utils.js` | `show`/`hide`/`setHTML`/`setText` تستخدم `document.getElementById`; إضافة `$` وتصديره |
| `backend/database/db.py` | `update_job` تسerialize الـ `dict`/`list` لـ JSON |
| `backend/services/ytdlp_service.py` | Timeout يستخدم إعدادات المستخدم بدلاً من hardcoded |
| `backend/routes/settings_api.py` | تغيير endpoint يتحقق من API key: `videos?chart=mostPopular` بدلاً من `channels?mine` |
| `frontend/index.html` | إضافة زر "💾 Save" بجانب Validate |
| `frontend/assets/js/settings.js` | إضافة handler لـ saveYoutubeKey |
| `frontend/assets/js/downloader.js` | playlist selector + channel download flow |

---

## 9. نصائح عامة

- **Hard refresh**: `Ctrl+Shift+R` بعد أي تعديل في frontend
- **إعادة تشغيل الباك اند**: `Ctrl+C` ثم `bash run.sh`
- **تفعيل YouTube API key**: اذهب إلى [Google Cloud Console](https://console.cloud.google.com/apis/library/youtube.googleapis.com) وتأكد إن YouTube Data API v3 مفعّلة
- **تثبيت wget**: `pkg install wget` (Termux) أو `apt install wget` (Linux)
- **للوصول من أجهزة أخرى**: استخدم `MEGADL_HOST=0.0.0.0 MEGADL_PORT=5000 python app.py`
